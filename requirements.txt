# ═══════════════════════════════════════════════════════════════════════════════
# IGNITE: Thermografische Entzündungsdetektion – Requirements
# ═══════════════════════════════════════════════════════════════════════════════

# KERN-DEPENDENCIES (immer installieren)
# ───────────────────────────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0          # Numerische Computations (FastMath, SIMD-Optimierungen)
opencv-python>=4.8.0        # Bild I/O, Morphologie, Connected Components
customtkinter>=6.0.0        # Modernes Dark-Mode GUI (CustomTkinter für schönere UI)
Pillow>=10.0.0              # Bild-Rendering & PNG-Export
reportlab>=4.0.0            # Klinische PDF-Befundberichte & Poster-Export
pydicom>=2.4.0              # DICOM-Export & Metadaten
scipy>=1.11.0               # Statistische Berechnungen & Signifikanztests

# DOKUMENTATION & TESTING (optional, aber empfohlen für Entwicklung)
# ───────────────────────────────────────────────────────────────────────────────
pytest>=7.4.0               # Automatisierte Unit-Tests für CI/CD-Validierung
matplotlib>=3.8.0           # Diagnostik-Visualisierung (kann durch custom-rendering ersetzt werden)

# GPU-BACKEND (optional – nur installieren wenn CUDA/NVIDIA-Grafikkarte vorhanden)
# ───────────────────────────────────────────────────────────────────────────────
# Installiere mit: pip install -e ".[gpu]" (oder pip install torch --index-url https://download.pytorch.org/whl/cpu)
# torch>=2.0.0              # GPU-beschleunigte Tensoroperationen (optional, via .[gpu])


# RUST-DEVELOPMENT (optional – nur für native Core-Kompilierung)
# ───────────────────────────────────────────────────────────────────────────────
#maturin>=1.4,<2.0         # Rust-PyO3 Compiler (wird durch cargo/maturin verwaltet)
